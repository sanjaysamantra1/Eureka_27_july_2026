How Javascript Works
====================
-When a JavaScript program runs, 1 Global Execution Context (anonymous) is created.
-For every function call, a new Function Execution Context (FEC) is created.
-Execution Context = the environment in which code executes.
-Each execution context has two phases: 
	1. creation phase. 
           a. memory is allocated for all variables(var,let,const)
           b. 'undefined' value is assigned to 'var' variables
	2. execution phase. (Code is executed line by line; variables receive their assigned values and functions are invoked)




Functions 
=========
1. Function Declaration
2. Function Expression
3. Arrow Function



Function Declaration  vs  Function Expression
=============================================
1. Function Declarations are named , Function Expressions are anonymous
2. Function Declarations are hoisted , Function Expressions are not hoisted




HigherOrder Function : A Function that takes another function as arguement
CallBack Function : A function that is passed as arguement to another function

   let arr = [10, 20, 30, 40, 50];
    // increas each value by 5 
    function incrementBy5(ele) {
        return ele + 5;
    }
    let res = arr.map(incrementBy5);
    console.log(res);
    // map() is higherorder function as it takes another function a s arguement
    // incrementBy5() is callback function as it is passed as arguement 




Recursion
=========
A function calling itself , is called recursive function



Assignment
===========
1. ask user to enter principal,time,rateOfInterest , calculate simpleInterest
2. enter a number and reverse that number
3. check if a number is pallindrome or not (121 is pallindrome)
4. find factorial of a number ( factorial of 5 is 120 )
5. check if a number is prime or not ( 3 is prime , 4 is not prime)