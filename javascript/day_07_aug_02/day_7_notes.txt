String
======
let str1 = 'Good Morning';
let str2 = "Good Morning";

ex: 'this is "javascript" class'   // valid
    "this is 'javascript' class"   // valid
    "this is "javascript" class"   //in-valid



Immutable
=========
let arr = [10,20,30,40]
arr[1] = 25;

    let str = 'Hello';  // string is immutable
    str[1] = 'f';
    str[2] = 'm';
    console.log(str); // Hello




String methods:
--------------
length;
toUpperCase();
toLowerCase();
charAt();
at()
charCodeAt();
concat();
indexOf();
lastIndexOf();
includes();
startsWith()
endsWith()
match();
matchAll()   // returns iterator
replace();
replaceAll();
repeat();
slice(start, end)
substring(start, end)
substr(start, length)
split()
search(regex)
trim()
eval(); (eval() is from Window Object)
localeCompare()

N.p:- substring() cannot accept negative indexes. slice() does.
